-- Cluster node map for interop acceptance against SkyJS nodes.
-- Consumed by the stock Lua node booted from the 3rd/skynet submodule
-- (CWD = 3rd/skynet; relative paths resolve from there).
skyjs = "127.0.0.1:2528"
